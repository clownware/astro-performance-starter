name: CI

on:
  # No push trigger: every commit reaching the default branch has already
  # run this pipeline on its PR, and deploys build independently, so the
  # post-merge rerun only duplicated the bill (~2,500 billed min/month
  # across the private clones of this template, Aug-Sep 2026).
  pull_request:
    branches: [master]
  # versions-sync.yml re-dispatches CI after it pushes a versions.json sync
  # commit to a Dependabot branch. That GITHUB_TOKEN push does create
  # pull_request runs, but GitHub holds them at action_required until someone
  # approves them; the dispatch gives build-test a result on the new head
  # without waiting for that click. See versions-sync.yml for the review step.
  workflow_dispatch:

# A newer push to the same PR supersedes the running one.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  security-events: write

jobs:
  build-test:
    runs-on: ubuntu-latest
    # Every job carries a hard cap: a hung step otherwise runs to GitHub's
    # 360-minute default and burns the account's minute budget.
    timeout-minutes: 20
    env:
      SITE_URL: https://${{ github.repository_owner }}.github.io

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version-file: '.nvmrc'

      - name: Setup PNPM
        uses: pnpm/action-setup@v6
        with:
          run_install: false

      - name: Get pnpm store directory
        shell: bash
        run: echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_OUTPUT
        id: pnpm-cache

      - name: Cache pnpm store
        uses: actions/cache@v6
        with:
          path: ${{ steps.pnpm-cache.outputs.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint, format & type-check
        run: pnpm run quality:ci

      - name: ADR enforcement suite (ADR-064, warn-only launch)
        run: pnpm run enforce

      - name: Unit tests with coverage
        run: pnpm run test:coverage

      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: coverage-report
          path: coverage/
          retention-days: 14
          if-no-files-found: ignore

      - name: Validate budget overrides
        run: pnpm run budgets:validate

      - name: Validate semantic color contrast
        run: pnpm run design:validate

      - name: Build site
        run: pnpm run build

      - name: Enforce JS bundle size budget
        shell: bash
        run: |
          set -e
          JS_BUNDLE_PATH="dist/_astro"
          JS_SIZE_LIMIT_BYTES=163840 # 160 KB raw JS
          if [ ! -d "$JS_BUNDLE_PATH" ]; then
            echo "JS bundle path not found. Skipping size check."
            exit 0
          fi
          JS_SIZE=$(find "$JS_BUNDLE_PATH" -name "*.js" -type f -exec stat -c%s {} + | awk '{sum+=$1} END {print sum}')
          JS_SIZE=${JS_SIZE:-0}
          echo "Total raw JS size: $JS_SIZE bytes (limit: $JS_SIZE_LIMIT_BYTES)"
          if [ "$JS_SIZE" -gt "$JS_SIZE_LIMIT_BYTES" ]; then
            echo "::error::JavaScript bundle size ($JS_SIZE bytes) exceeds limit ($JS_SIZE_LIMIT_BYTES bytes)"
            exit 1
          fi

      - name: Enforce raw-size budgets — budgets.json (with budget-overrides applied)
        run: pnpm run perf:budgets

      - name: Enforce per-image size budget — source (ADR-057)
        run: pnpm run images:gate

      - name: Enforce per-image size budget — build output (ADR-057)
        # Catches oversized emitted raster, e.g. heavyweight PNG fallbacks
        # generated alongside AVIF/WebP.
        run: IMAGE_GATE_ROOTS=dist pnpm run images:gate

      - name: Enforce font preload budget (ADR-058)
        run: pnpm run fonts:gate

      # Chromium is ~130MB per run uncached; key on the resolved Playwright
      # version so the cache invalidates exactly when the browser build does.
      # System libraries can't live in the cache, so a hit still runs
      # install-deps — but both apt passes carry a step timeout: the runner's
      # apt mirror (azure.archive.ubuntu.com) stalled `apt-get update` for
      # 20+ minutes on 2026-08-19 and only the job cap ended it. On a cache
      # hit the pass is also continue-on-error: ubuntu-latest already ships
      # Chromium's shared libraries (Chrome is preinstalled), and a genuinely
      # missing library fails the E2E step visibly a moment later.
      - name: Get Playwright version
        id: playwright-version
        run: echo "version=$(node -p "require('@playwright/test/package.json').version")" >> "$GITHUB_OUTPUT"

      - name: Cache Playwright browsers
        id: playwright-cache
        uses: actions/cache@v6
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ steps.playwright-version.outputs.version }}

      # Browser download comes from Playwright's CDN — no apt on the critical path.
      # (`--with-deps` ran apt first; the runner's apt mirror stalled it for 5–20 min
      # repeatedly on 2026-08-19.)
      - name: Install Playwright browsers
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        timeout-minutes: 5
        run: pnpm exec playwright install chromium

      # Safety net on both cache paths: ubuntu-latest already ships Chromium's shared
      # libraries (Chrome is preinstalled), so a stalled apt mirror must not fail the
      # job — capped and tolerated; a genuinely missing library fails the E2E step
      # visibly a moment later.
      - name: Install Playwright OS dependencies
        timeout-minutes: 3
        continue-on-error: true
        run: pnpm exec playwright install-deps chromium

      - name: Run E2E tests (Chromium)
        run: pnpm exec playwright test --project=chromium

      - name: Security audit (high severity)
        run: pnpm run audit:ci

      - name: Trivy SBOM scan
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: 'fs'
          format: 'sarif'
          output: 'trivy-results.sarif'

      - name: Upload security results
        if: success() && (hashFiles('trivy-results.sarif') != '')
        uses: github/codeql-action/upload-sarif@v4
        continue-on-error: true
        with:
          sarif_file: 'trivy-results.sarif'


  # SAST + secret scanning (ADR-046). Both are halt-on-violation gates (ADR-039):
  # each command exits non-zero on a finding and fails the build.
  #
  # One job, not two: GitHub bills every job rounded UP to a whole minute and
  # each scan finishes in ~20 s, so the earlier semgrep + gitleaks pair paid two
  # billed minutes per run for ~40 s of work (~2 min x every CI run). Both tools
  # still run from their official images, via `docker run` instead of a job
  # container — Semgrep's docs mandate the semgrep/semgrep image, and the
  # gitleaks CLI avoids the gitleaks-action org-license requirement.
  scan:
    name: SAST + secret scan
    runs-on: ubuntu-latest
    timeout-minutes: 10
    # Dependabot PRs only touch manifests and run with a restricted token; skip.
    if: github.actor != 'dependabot[bot]'
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Semgrep scan
        # --error exits 1 on findings; --config p/* pulls public registry rulesets.
        run: >-
          docker run --rm -v "$PWD:/src" semgrep/semgrep
          semgrep scan --config p/javascript --config p/typescript --config p/secrets --error

      - name: Gitleaks scan
        # `dir` scans the working tree; --exit-code 1 fails the build on any leak.
        run: >-
          docker run --rm -v "$PWD:/repo" -w /repo ghcr.io/gitleaks/gitleaks:v8.30.1
          dir . --config .gitleaks.toml --exit-code 1 --verbose
